"""Backward-compatibility re-export shim.

DEPRECATED — this file is empty. All tasks have been migrated to their
domain-specific modules:
  - pipeline_tasks.py  — enrichment, scoring, analysis, drafting
  - research_tasks.py  — Scout research, investigation
  - review_tasks.py    — LLM review tasks
  - batch_tasks.py     — batch processing
  - brief_tasks.py     — commercial brief generation
  - crawl_tasks.py     — Google Maps crawling
  - weekly_tasks.py    — periodic report generation
  - janitor.py         — cleanup and maintenance

Import directly from the domain file instead.
"""
